window.WEB_NOTEPAD_CONFIG = {
  // Global default OAuth Client ID for all environments (optional)
  driveClientId: "",
  // OAuth Client ID used only when running on Chromebook (optional)
  chromebookDriveClientId: ""
};
